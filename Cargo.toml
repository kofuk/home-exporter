[package]
edition = "2024"
name = "home-exporter"
version = "0.1.0"
license = "MIT"

publish = false

[dependencies]
bt-hci = { version = "0.6", default-features = false, features = ["defmt"] }
chrono = { version = "0.4.43", default-features = false, features = ["defmt"] }
cortex-m-rt = "0.7.0"
cyw43 = { version = "0.6.0", features = ["defmt", "bluetooth", "firmware-logs"] }
cyw43-pio = { version = "0.9.0", features = ["defmt"] }
defmt = "1.0.1"
defmt-rtt = "1.0.0"
embassy-executor = { version = "0.9.0", features = ["arch-cortex-m", "executor-thread", "executor-interrupt", "defmt"] }
embassy-net = { version = "0.8.0", features = ["defmt", "icmp", "tcp", "udp", "raw", "dhcpv4", "medium-ethernet", "dns"] }
embassy-net-driver-channel = "0.3.2"
embassy-rp = { version = "0.9.0", features = ["defmt", "unstable-pac", "time-driver", "critical-section-impl", "rp235xa", "binary-info"] }
embassy-sync = { version = "0.7.2", features = ["defmt"] }
embassy-time = { version = "0.5.0", features = ["defmt", "defmt-timestamp-uptime"] }
embedded-nal-async = "0.9.0"
heapless = { version = "0.9", features = ["serde"] }
linked_list_allocator = "0.10.5"
panic-probe = { version = "1.0.0", features = ["print-defmt"] }
reqwless = { version = "0.14.0", features = ["rsa"] }
serde = { version = "1.0.203", default-features = false, features = ["derive"] }
serde-json-core = "0.5.1"
static_cell = "2.1"
trouble-host = { version = "0.5.1", features = ["scan", "default-packet-pool-mtu-128"] }

[features]
default = ["remote-write"]
bluetooth = []
wifi = []
ntp = []
sbmeter = ["bluetooth"]
remote-write = ["wifi", "ntp"]

[package.metadata.embassy]
build = [
  { target = "thumbv8m.main-none-eabihf", artifact-dir = "out" }
]
